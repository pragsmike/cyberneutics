# Agent Metadata

This directory holds operational repo metadata for agents working inside Cyberneutics: handoffs, rosters, prompts, rubrics, diary notes, and active plans.

It does not hold live runtime outputs. Checked-in scenario and deliberation records live under `examples/`. `agent/archive/` is historical only and should be excluded from onboarding and normal repo search unless provenance is explicitly needed.