# Examples

This directory holds checked-in scenario and deliberation records that remain in the repo as examples or historical references.

- `examples/scenarios/` contains example scenario-generation runs.
- `examples/deliberations/` contains example committee and review runs.

These are not live runtime output locations. New runs belong in external situation directories. Prose walkthroughs and worked-example writeups remain under `artifacts/examples/`.